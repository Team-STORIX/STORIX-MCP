export const GUIDE = `API가 기대와 다르게 동작할 때 쓴다. 서로 넘겨짚기 전에 스펙으로 먼저 가린다.

## 순서

1. report_check 로 판정한다. 스펙이 정답지라 상당수는 여기서 갈린다.
2. 판정을 사용자에게 보여준다.
3. 진짜 문제로 보이면 report_send 로 슬랙에 올린다. 보내기 전에 내용을 확인받아라.

## 판정이 무엇을 보는가

요청이 스펙과 다르면 프론트 - 필수 필드 누락, 타입 불일치, enum 위반, 경로 오타.
응답이 스펙과 다르면 백엔드 - 문서에 없는 상태값, 스펙에 없는 에러 코드, 응답 필드 불일치.

가리지 못하는 경우도 있다. 그때는 단정하지 않는다.

- 401/403 인증·권한. 토큰이 유효한지, 필요한 권한을 가진 계정인지 먼저 확인한다
- 문서화된 에러가 그대로 나온 경우. 비즈니스 규칙에 걸린 정상 동작일 수 있다
- 스펙에 아예 없는 경로. 아직 배포 안 됐거나 제거된 것일 수 있어 swagger_history 로 확인한다

## 보내기 전에

symptom 은 한 줄로 구체적으로 쓴다. "안 돼요" 말고 "생성 버튼 누르면 400 뜸".
요청·응답을 같이 넣으면 판정이 훨씬 정확해진다. 넣을 수 있으면 넣어라.

판정이 "확인필요"로 나왔는데도 올릴 수 있다. 스펙으로 못 가린다는 것이지 문제가 없다는 뜻이 아니다.
다만 그때는 symptom 에 상황을 더 자세히 적어라 - 사람이 읽고 판단해야 하니까.

먼저 dryRun 으로 올라갈 내용을 보여주고 확인받는 편이 낫다.`;
